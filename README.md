## Instructions

- Clone to `~/.config/nvim`
- Have the following installed:
  - `ripgrep`
  - `node` (use nvm or volta)
  - `unzip`

### Recommended

In `.zshrc` or whatever:

```sh
export EDITOR=nvim
alias vi=nvim
alias vim=nvim
alias oldvim=vim
```
